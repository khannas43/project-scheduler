# Copy to `.env.sonar` (gitignored) for local scans against SonarQube.
# Create the token in SonarQube → My Account → Security → Generate Tokens
# (type: User Token, or Global Analysis Token).

SONAR_HOST_URL=http://localhost:9012
SONAR_TOKEN=squ_your_token_here
